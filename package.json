{
  "name": "canvas-kit",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @canvas2026/shared && npm run build -w @canvas2026/converter && npm run build -w @canvas2026/client && npm run build -w @canvas2026/canvas-mcp",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.5"
  }
}
